/*
    cortex_resource.qc - Cortex Bot v2 Resource Strategy

    Currently the heavy scoring lives in cortex_bot.qc (FindBestPickup + respawn inference).
    This file exists as the module boundary for future expansion.
*/

// Intentionally empty for now.

